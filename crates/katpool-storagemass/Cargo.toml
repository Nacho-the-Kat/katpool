[package]
name = "katpool-storagemass"
description = "KIP-9 and KIP-13 storage- and transient-mass calculator. Pure functions; heavily property- and fuzz-tested."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

# Scaffold crate — deps reserved for Phase 4-5 storage-mass /
# transient-mass calculators (KIP-9 + KIP-13).
[package.metadata.cargo-machete]
ignored = ["katpool-domain", "thiserror"]

[dependencies]
katpool-domain = { path = "../katpool-domain" }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
